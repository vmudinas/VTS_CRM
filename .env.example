#########
# Environment variables for docker-compose
#########
# SA_PASSWORD must meet SQL Server strong password requirements:
# at least 8 characters, with uppercase, lowercase, numbers, and symbols
SA_PASSWORD=YourStrong!Passw0rd

# DB_PASSWORD for the API to connect; can reuse SA_PASSWORD or set separately
DB_PASSWORD=${SA_PASSWORD}


# React will pick this up in dev mode
               REACT_APP_API_URL=http://localhost:4000/api

               # .NET API settings
               ASPNETCORE_URLS=http://+:4000
# Database Configuration

# JWT Secret for Authentication
JWT_SECRET=foldsandflavors-secret-key-2025
# Database configuration for prod environment (port 1433)
DB_USER=sa
DB_SERVER=localhost
DB_NAME=FoldsAndFlavors
DB_PORT=1433
